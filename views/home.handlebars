
  <div class="banner">
    <span>
      <h1>MEU IP</h1>
    </span>
  </div>
  
  
  <div class="geral">
    <div class="painel">
    
    <pre>
    IP DE ORIGEM: {{ip}}
    </pre>

    <div id="local">
    <pre>
    CIDADE: {{dados.cityName}}<br>
    PAÍS: {{dados.countryName}}
    </pre>
    </div>  

    <div class="dados">
    <pre>
      SISTEMA OPERACIONAL: {{result.sistemaOperacional}}<br>
      CLIENTE: {{result.navegador}}
      </pre>
    </div>
  </div>
  
   <br><br>

  <div id="map"></div>
  <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
  <script>

    var latitude = {{dados.latitude}};
    var longitude = {{dados.longitude}};    


    var map = L.map('map').setView([latitude, longitude], 5);
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors',
      maxZoom: 18,
    }).addTo(map);
    L.marker([latitude, longitude]).addTo(map);
  </script>
  <br>







